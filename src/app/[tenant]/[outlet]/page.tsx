import Store from '@/Components/Tenant/Store'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <Store />
        </div>
    )
}

export default page